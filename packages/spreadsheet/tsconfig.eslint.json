{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // ensure that nobody can accidentally use this config for a build
        "noEmit": true,
        "emitDeclarationOnly": true
    },
    "include": ["src", "stories", "tests", "*.config.mts", ".storybook/main.js", ".storybook/preview.jsx"]
}
