{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // ensure that nobody can accidentally use this config for a build
        "noEmit": true,
        "emitDeclarationOnly": true
    },
    "include": ["src", "stories", "tests", "*.config.ts", ".storybook/*.ts"]
}
